Extract the following information from this construction contractor payment-application document and provide the result in JSON format:
{
    "agreement_parties": {
        "prime_contractor": "" [string]
    },
    "application_for_payment_summary": {
        "application_for_payment_number": "" [string],
        "application_for_payment_date": "" [string, "YYYY-MM-DD" format],
        "work_completed_this_application": "" [float]
    }
}
- The JSON schema must be followed during the extraction. This nesting is intentional — "prime_contractor" belongs under "agreement_parties", and the other three fields belong under "application_for_payment_summary", matching how these concepts are grouped in the taxonomy this tool tags against.
- The values must only include text or numbers found in THIS SPECIFIC document. Every value shown below as an example (company names, numbers, dates) is a made-up placeholder used ONLY to illustrate format and label patterns — none of them are real answers, and none belong to this or any other document. NEVER output an example value verbatim. If a real value is hard to read (wrapped across lines, split by a page layout, partially obscured), read it carefully from the actual document rather than falling back to an example shown here.
- If a field is not found in the document, set the value to N/A.
- These documents come from different state DOT (Department of Transportation) contractor-payment reporting systems (e.g. Caltrans, Iowa DOT, TxDOT SiteManager) and may come from other similar contractor payment-application formats in the future. Field labels vary by system — recognize all listed synonyms below, not just one exact phrase. Do not assume every document uses the same layout.
- prime_contractor: The general/prime contractor named on the document — not a subcontractor, and not the issuing government agency. Usually appears near the top of the document. Sometimes explicitly labeled ("CONTRACTOR:"), sometimes printed directly after a contractor number with no explicit label — format pattern only: "Contractor No. <number> <PLACEHOLDER COMPANY NAME> <CITY, STATE>" → prime_contractor is "<PLACEHOLDER COMPANY NAME>" (a made-up example; read the real company name from this document, even if it wraps across multiple lines or is interrupted by address/location text in the layout). Extract the company name only, not the address that follows it.
- application_for_payment_number: The sequential number identifying which payment application / estimate / voucher this is. Recognized labels include "EST. NO.", "ESTIMATE NUMBER:", "ESTIMATE", and "Voucher". Extract the bare identifier exactly as printed after that label — a short numeric or alphanumeric code, varying in length by system. Do not default to any number shown elsewhere in these instructions; read the specific digits printed on this document.
- application_for_payment_date: The date the payment application/estimate was issued or reported. Recognized labels include "DATE OF THIS ESTIMATE", "REPORT DATE:", and "Date" (printed near a voucher number). Convert to "YYYY-MM-DD" format regardless of the printed format (e.g. a printed "MM/DD/YY" or "MM-DD-YY" becomes "20YY-MM-DD" — this is a format-conversion rule, not an example date to copy). Do not confuse this with other dates that may appear on the document (award date, letting date, notice-to-proceed date, work-begin date, physical-completion date) — only the date the estimate/application itself was issued or reported.
- work_completed_this_application: The dollar amount of work completed / earned / paid for THIS SPECIFIC payment period — NOT the cumulative to-date total, even when the two happen to be equal (e.g. on a document's very first estimate). Different systems label the relevant column differently:
  - Caltrans-style reports: the "THIS ESTIMATE" column under "SUBTOTAL AMOUNT EARNED" / "SUBTOTAL CONTRACT ITEMS" (distinct from the "TOTAL ESTIMATE" cumulative column).
  - Iowa-DOT-style vouchers: the "Amount This Period" column under "Totals For Summary" (distinct from "Amount To Date"). This can legitimately be 0.00 on a voucher with no new billable work that period (e.g. a retainage-only adjustment) — extract 0.00 rather than treating it as missing.
  - TxDOT SiteManager-style estimates: the "THIS ESTIMATE" column under "PAID TO CONTRACTOR" / "RECAPITULATION" (distinct from "TOTAL TO DATE" and "PREV TO DATE").
  Extract as a plain number without currency symbols or thousands separators.
Return ONLY the JSON. Do not add any explanation or text outside the JSON.
